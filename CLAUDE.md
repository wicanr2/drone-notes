# CLAUDE.md — drone-notes 專案工作規則

這個 repo 是一份**寫給軟體工程師的無人機系統專論**。讀者是會寫服務端、會用 Docker、懂 CI 的工程師,但沒碰過飛控、沒接過 MAVLink、不知道哪些事情不能塞進即時控制迴圈。文件要把「為什麼是這個設計」講到能自己推導,而不是列出一堆名詞與步驟。

## 寫作規則

**第一性原理優先。** 每個概念先回答「要解決什麼根本問題」「為什麼非這樣不可」,再談怎麼用。會動到公式的地方(座標轉換、互補濾波、控制律、頻寬預算)把式子逼出來,不要先給結論再解釋符號。

**柵欄原則。** 解釋既有設計(MAVLink 的 8-bit sysid、PX4 的 uORB、NED 座標)時,先重建它當初擋住了什麼問題。看起來不合理的規格通常是某個隱藏約束演化出來的解,不要寫成「前人設計得差」。

**不臆造。** 版本號、API 名稱、參數名、專案存活狀態一律以官方來源為準。查不到就寫「待查證」,不要生一個看起來合理的名字。所有版本事實集中在 `CONTEXT.md` 的版本表,附查證日期;文件內文引用該表,不要各自寫一套版本號。

**誠實標界線。** 分清楚 roadmap 與現成、官方與社群、模擬與實機、我驗過與我讀到的。把「可以這樣做」寫成「已經有人這樣做」是這份文件最容易犯的錯。

**繁體中文、中性技術風格。** 術語第一次出現當場一句話翻譯,之後直接用原文。程式碼、識別符、參數名保留原文。避免浮誇詞(強大、極致、大幅提升、賦能、打造、無縫、生態)與導引式後設句(「先看這段」「本文將介紹」「用途:給 XX 看」)。

**結論先行。** 每篇開頭一段講清楚這篇要回答什麼問題、答案是什麼,再展開推導。每組數字後面接一句「所以呢」。

## 目錄與命名

```
README.md            索引與閱讀動線
CONTEXT.md           術語表 + 版本現況查證表 + 表述決策
PLAN.md              分輪進度與待辦
docs/NN-topic/*.md   數字前綴決定閱讀順序,每檔一個主題
docs/NN-topic/appendix/  產生出來的查詢用參考,不進閱讀動線
img/*.svg            所有手繪圖
reference-impl/      可跑的最小參考實作
tools/               文件產生器
```

路徑用英文 kebab-case,標題與內文用繁中。檔名前綴的數字是閱讀順序,不是重要性排序。

## 圖的分界

- **Mermaid**:結構關係、流程、時序、狀態機。GitHub 原生渲染,改起來便宜,預設用它。
- **手繪 SVG**:Mermaid 表達不了的——座標系與旋轉、控制迴圈的時間軸與取樣率、頻寬與延遲的量級對比、左壞右好的對照圖。放 `img/`,文件用相對路徑引用(`docs/NN-topic/` 深度 2 → `../../img/`)。

SVG 規則:白底、克制配色、圓角框,`font-family="'Noto Sans CJK TC','PingFang TC','Microsoft JhengHei',sans-serif"`。曲線用 awk 算 path 點。**畫完一定轉 PNG 自檢**:

```bash
google-chrome --headless --no-sandbox --disable-gpu --screenshot=/tmp/x.png \
  --window-size=W,H --force-device-scale-factor=2 "file://$PWD/img/x.svg"
```

用 Read 看 PNG,有文字重疊、截斷、比例誤導就修再驗。

## API 參考:不手抄,要收就留產生器

原則是**不把 API 快照手抄進 repo**——手抄的東西會過期,而且過期的時候看不出來。但這不等於不能收 API 參考,而是收的方式要不同:

- **留產生器,不留手抄稿。** 產生器放 `tools/`,產物放對應章節的 `appendix/`。產物開頭一定寫明由哪支腳本、從哪個釘死的版本產生。
- **釘版本。** 標題帶版本號,出處表寫清楚原始碼的 tag 或套件版本。
- **產物不手改。** 要改就改產生器再重跑。
- **解析涵蓋率要誠實。** 抓不到的、原始碼裡就沒說明的,照實標出來,不代為推測。

收與不收的判準是**有沒有官方文件給不了的東西**:版本對得上、分得出哪些是穩定契約、可以 grep 與 diff。純粹重複官方參考的內容不收。

現有的三處:[MAVSDK 介面全貌](docs/20-protocols/03-mavsdk-api-surface.md)(套件無官方參考)、[PX4 附錄三份](docs/10-flight-controller/appendix/)(版本對齊與契約分區)。

## reference-impl 規則

- 編譯與執行一律走 Docker,Python 用容器內 `uv venv`,不污染系統環境。
- 骨架要能真的跑起來,並附一條驗證指令證明它跑起來了。跑不起來的範例不要進 repo。
- 文件裡提到的架構要能對到實際檔案路徑;改了程式碼就同步改文件。

## 每篇文件的驗收標準

- [ ] 開頭一段講清楚要回答什麼問題,答案是什麼
- [ ] 每個關鍵設計都能回答「為什麼不是別的做法」
- [ ] 版本、API、參數名有官方來源;不確定的標「待查證」
- [ ] 術語第一次出現有一句話解釋,且已收進 `CONTEXT.md`
- [ ] 至少一張圖,且圖與文字說的是同一件事
- [ ] 沒有導引式後設句、沒有浮誇詞
- [ ] `README.md` 索引與 `PLAN.md` 進度已同步更新

## Commit

繁中訊息,一輪一個主題,結尾加 `Co-Authored-By`。每輪結束就 push,不要累積一大包。
